{
  "buildCommand": "npx prisma generate && (if [ -n \"$DATABASE_URL\" ]; then npx prisma migrate deploy; else echo 'DATABASE_URL is not set, skipping migration'; fi) && next build",
  "framework": "nextjs"
}
